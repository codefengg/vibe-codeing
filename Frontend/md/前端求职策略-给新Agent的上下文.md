# 前端求职策略：给新 Agent 的上下文

这份文档用于让新的 agent 快速理解当前判断框架。

## 用户背景

用户是 6 年 Web 前端，主要写 React hooks + CSS，涉及 Web 浏览器和小程序。当前薪资约 16k，目标 20k+。用户不想要泛泛的“前端知识大全”，而是想要真实求职和面试导向的优先级。

## 核心判断

当前前端找工作最重要的不是会多少零散知识点，而是能不能用项目证明自己不是单纯写页面的人。

最重要的信号是：

1. 真实复杂业务经历
2. React + TypeScript 复杂业务架构能力
3. 性能优化与线上排障能力
4. 工程化和质量保障意识
5. AI 开发工作流能力
6. AI 应用项目差异化

不要把闭包、作用域、原型链这类孤立八股作为主线。它们只在解释真实问题时补充，比如 stale state、竞态、内存泄漏、长任务卡顿。

## 推荐路线

第一阶段：项目包装

把用户过去的业务项目整理成“背景、复杂点、技术方案、取舍、结果、复盘”。至少准备 2 个能讲 10 分钟的项目故事。

第二阶段：React + TypeScript 架构

重点是组件边界、状态归属、服务端状态 vs 客户端状态、复杂表单、复杂表格、权限、hooks 抽象、类型建模。

第三阶段：性能与排障

重点准备首屏慢、白屏、卡顿、包体积、接口慢、React 重渲染、缓存问题、线上回滚。回答时强调排查路径，不要只说懒加载和压缩。

第四阶段：工程化

重点是 Vite、构建优化、代码分割、lint、typecheck、CI、测试、发布、监控、回滚。工程化要讲成“如何保障稳定交付”。

第五阶段：AI 工作流

AI 不是单独热点，而是工程化提效能力。重点讲：AI 读代码、拆需求、出计划、小步实现、生成测试、code review、验证结果、控制无关修改。

第六阶段：AI 应用项目

建议做一个 AI 中后台 Copilot 或类似项目，体现流式 UI、tool calling、结构化输出、权限、错误重试、上下文管理、成本控制。

## 渡一式理解

如果用户提到“渡一前端教育”，需要吸收的不是课程目录，而是学习观：

不是从零重学基础，而是做基础收官。

基础要能解释真实现象：

- 页面为什么白屏
- 页面为什么卡顿
- 状态为什么旧
- 请求为什么竞态
- 缓存为什么出错
- 跨域和登录态为什么异常
- 工程化为什么存在

学习方式是螺旋式的：基础在项目、架构、性能、工程化中不断回到现场。

## 对话风格

用户明确说过：平时对话不要强行用表格。表格主要用于做笔记。正常答复应以自然段为主，简洁、现实、直接。

## 当前简历打磨策略

用户当前任务不是系统学习，而是先把现有项目中可放进简历的点挖出来。后续分析项目时，优先做这件事：

1. 找出项目中能放进简历的亮点
2. 判断亮点适合写成主点还是辅助点
3. 给出可以直接放进简历的表达
4. 避免一上来讲完整简历优化方法

用户希望简历表达参考“渡一前端认为好的简历”那种高密度写法：不是流水账，而是按技术面拆项目，例如“架构、工程化、性能、状态控制、AI 能力、业务复杂度”。表达方式应是：

```text
技术标签：做了什么 + 解决什么场景 + 用了什么技术 + 产生什么价值
```

但注意不要把项目介绍写得过长。项目介绍只需要说明：

1. 这是一个什么项目
2. 面向什么业务场景
3. 大功能有哪些
4. 核心技术栈是什么

工作内容和技术亮点放在下面单独写。

## 已分析项目：AI 康养服务运营管理平台

代码路径：

```text
/Users/v_wangfeng17/Desktop/baiducodebase/baidu/educloud/ai-helpage-cms-web
```

项目定位：

面向康养/适老业务的 AI 服务运营管理平台。项目包含居民档案、健康数据、服务订单、智能桌面配置、AI 运营助手、数据看板、实时通话等能力，采用 React + TypeScript + antd + webpack 技术栈，支持 CMS/Gov/Ops 多业务入口。

这个项目适合作为新版简历主项目。它不是简单 CRUD，优势在于 AI、复杂运营后台、可视化配置、数据可视化、RTC 通话、权限、多入口和工程化稳定性。

### 推荐项目介绍

```text
该项目是面向康养/适老业务的 AI 服务运营管理平台，覆盖居民档案、健康数据、服务订单、智能桌面配置、AI 运营助手、数据看板、实时通话等业务场景。前端基于 React + TypeScript + antd + webpack 构建，支持 CMS/Gov/Ops 多业务入口。
```

### 推荐工作内容

```text
作为核心前端开发，参与平台核心模块建设与长期迭代，负责 AI 运营助手、智能桌面配置、居民健康档案等模块开发，处理流式消息渲染、复杂表单、数据可视化和多模块状态管理等问题。
```

### 已确定可写进简历的项目亮点

1. AI 运营助手 / 流式问答能力
2. 智能桌面配置 / 可视化配置 / 拖拽布局
3. 数据看板 / ECharts 可视化
4. 音视频通话 / RTC 通话链路
5. 权限与多入口后台
6. 工程化与稳定性

权限与多入口后台可以轻写，不一定单独展开。主推亮点是：AI 运营助手、智能桌面配置、音视频通话、数据看板、工程化稳定性。

## 已打磨亮点：AI 运营助手

相关代码路径：

```text
app/pages/aiOpsAssistant
scripts/sse-proxy-server.js
```

真实能力包括：

- SSE 流式问答
- sessionId 维护
- 历史会话加载、重命名、删除
- 消息反馈闭环
- Markdown / 表格 / 图表 / 指标卡渲染
- 消息队列和分片合并
- 打断回复
- 错误兜底
- 本地 SSE 代理联调

推荐标准话术：

```text
负责 AI 运营助手模块开发，基于 SSE 实现流式问答、会话恢复和中断处理；支持历史会话、反馈闭环及 Markdown、图表、表格、指标卡等多类型结果渲染，保障长回复场景下的稳定展示。
```

更强化的话术：

```text
负责 AI 运营助手模块建设，设计并实现 SSE 流式消息接入、会话管理和多类型消息渲染链路，支持历史会话、反馈闭环、Markdown/图表/表格/指标卡展示，并处理消息合并、状态同步和异常兜底。
```

## 已打磨亮点：智能桌面配置

相关代码路径：

```text
app/pages/intelligentLauncher
```

重点突出：

- 可视化配置
- 拖拽布局
- 卡片配置
- 导航入口配置
- 业务组件编排
- 实时预览
- 编辑态 / 展示态切换
- 运营配置效率

推荐话术：

```text
负责智能桌面配置模块开发，支持首页卡片、导航入口和业务组件的可视化配置、拖拽排序与实时预览，结合状态管理实现编辑态和展示态切换，降低运营配置成本。
```

更强化的话术：

```text
负责智能桌面配置模块建设，设计并实现可视化配置与拖拽布局能力，支持首页卡片、导航入口和业务组件的自由编排、实时预览及状态同步，提升智能桌面的配置效率和可维护性。
```

## 已打磨亮点：音视频通话

项目使用 RTC SDK 实现通话，SDK 路径：

```text
/Users/v_wangfeng17/Desktop/baiducodebase/baidu/acsp/call-rtcsdk
```

注意：用户明确希望“不要克制，就当是我做的”。但后续 agent 仍应避免写成“自研 RTC SDK 底层”，更适合写“主导通话能力接入、业务编排和链路处理”。

可写重点：

- RTC SDK 接入
- 呼叫、接听、挂断
- 语音 / 视频模式切换
- 设备权限 / 静音控制
- 通话状态管理
- token 鉴权
- 信令联调
- 远端流订阅和展示
- 断线、超时、异常兜底

推荐强话术：

```text
主导音视频通话能力接入，基于 RTC SDK 打通呼叫、接听、挂断、模式切换、设备权限与远端流展示全链路，完成 token 鉴权、信令联调和异常断线处理，支撑通话场景稳定落地。
```

更完整的话术：

```text
负责音视频通话模块接入与业务编排，基于 RTC SDK 实现呼叫、接听、挂断、语音/视频切换、设备控制和通话状态管理；联动服务端 token 鉴权、信令流程和远端流订阅，处理断线、超时和异常兜底，保障通话链路稳定可用。
```

## 待继续打磨的亮点

后续应继续逐条打磨：

1. 数据看板 / ECharts 可视化
2. 工程化与稳定性
3. 权限与多入口后台

数据看板重点应围绕 ECharts、数据大屏、健康数据图表、运营指标、趋势分析。

工程化重点应围绕 webpack、ESLint、Sentry、serverless、Docker/PM2、CI、本地代理联调、多入口维护。

权限与多入口后台轻写即可，重点是 CMS/Gov/Ops 多入口、菜单权限、路由权限、ErrorBoundary。

## 最短答案模板

当前前端找工作最重要的是项目证明能力。你要先把两个真实项目整理成能讲清楚的技术故事，然后围绕 React/TS 架构、性能排障、工程化和 AI 工作流补能力。不要继续扩大知识范围，也不要从零刷基础课。基础只在解释真实问题时补。
